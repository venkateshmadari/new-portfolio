/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com', "p1.hiclipart.com", 'w7.pngwing.com', 'assets.aceternity.com'],
    },
};

export default nextConfig;
